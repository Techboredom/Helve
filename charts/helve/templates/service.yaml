apiVersion: v1
kind: Service
metadata:
  name: {{ include "helve.fullname" . }}
  labels:
    {{- include "helve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "helve.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
