{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "helve",
  "type": "object",
  "properties": {
    "image": {
      "type": "object",
      "properties": {
        "repository": { "type": "string", "description": "Container image repository, e.g. ghcr.io/<owner>/helve." },
        "tag": { "type": "string", "description": "Image tag. Defaults to .Chart.AppVersion when empty." },
        "pullPolicy": { "type": "string", "enum": ["Always", "IfNotPresent", "Never"] },
        "pullSecrets": { "type": "array", "items": { "type": "string" }, "description": "Names of existing image-pull Secrets." }
      }
    },
    "host": {
      "type": "string",
      "description": "Required. The public hostname Helve is served from, e.g. helve.example.com. The chart refuses to render without this set."
    },
    "replicaCount": {
      "type": "integer",
      "minimum": 1,
      "description": "Safe above 1: session/proxy state lives in Postgres and quota enforcement uses a Postgres advisory lock, not in-process state."
    },
    "terminationGracePeriodSeconds": {
      "type": "integer",
      "minimum": 1,
      "description": "Must comfortably exceed the app's own ~5s post-SIGTERM wait plus real request completion time. Does not help already-open long-lived connections (proxied app sessions, the Pods tab's live-update WebSocket)."
    },
    "proxy": {
      "type": "object",
      "properties": {
        "baseDomain": {
          "type": "string",
          "description": "Base domain per-deployment proxy origins are served under (\"<name>.<baseDomain>\"). Defaults to \"proxy.<host>\" when empty. Needs a wildcard DNS record for \"*.<baseDomain>\"."
        },
        "separateOrigins": {
          "type": "boolean",
          "description": "Serve each proxied deployment from its own origin (recommended, default true). Setting this false is only accepted alongside allowSameOriginProxy=true."
        },
        "allowSameOriginProxy": {
          "type": "boolean",
          "description": "Explicit acknowledgment required to set proxy.separateOrigins=false, since that lets a proxied app's JavaScript call Helve's own API as whoever is browsing it."
        }
      }
    },
    "ingress": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "className": { "type": "string" },
        "annotations": { "type": "object" },
        "tls": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "mode": {
              "type": "string",
              "enum": ["certManager", "existingSecret", "none"],
              "description": "certManager renders a Certificate; existingSecret uses tls.secretName as-is; none leaves TLS to something in front of this chart."
            },
            "secretName": { "type": "string" },
            "issuerRef": {
              "type": "object",
              "properties": {
                "name": { "type": "string", "description": "Required when tls.mode=certManager." },
                "kind": { "type": "string", "enum": ["ClusterIssuer", "Issuer"] }
              }
            }
          }
        }
      }
    },
    "database": {
      "type": "object",
      "properties": {
        "existingSecret": {
          "type": "string",
          "description": "Name of an existing Secret holding a Postgres connection string. The supported path — either this or database.deploy.enabled must be set."
        },
        "existingSecretKey": { "type": "string", "description": "Key within existingSecret holding the connection URI." },
        "deploy": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Evaluation only. Deploys a single-replica, no-backups CloudNativePG Postgres alongside Helve. Requires the CloudNativePG operator already installed on the cluster. Do not use for anything you'd be upset to lose."
            },
            "storage": {
              "type": "object",
              "properties": {
                "size": { "type": "string" },
                "className": { "type": "string", "description": "StorageClass name; empty uses the cluster default." }
              }
            }
          }
        }
      }
    },
    "homeDrives": {
      "type": "object",
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["off", "hostPath", "pvc"],
          "description": "off (default): no home-directory feature. hostPath: mount <hostPath.basePath>/<username> from every node's own filesystem. pvc: see pvc.strategy for the two ways this mode can provide storage."
        },
        "hostPath": {
          "type": "object",
          "properties": {
            "basePath": {
              "type": "string",
              "description": "Required when mode=hostPath. Every node must already have the same shared filesystem mounted at this path at the OS level."
            }
          }
        },
        "pvc": {
          "type": "object",
          "properties": {
            "strategy": {
              "type": "string",
              "enum": ["dynamic", "shared"],
              "description": "dynamic (default): Helve creates one PersistentVolumeClaim per user from storageClassName/size. shared: Helve mounts existingClaimName (provisioned by you) into every user's pod with subPath: <username>, creating nothing itself."
            },
            "storageClassName": {
              "type": "string",
              "description": "Required when mode=pvc and strategy=dynamic. Must support ReadWriteMany if a user can run more than one home-drive environment at once."
            },
            "size": { "type": "string", "description": "Size of each per-user home PVC. Only meaningful with strategy=dynamic." },
            "existingClaimName": {
              "type": "string",
              "description": "Required when mode=pvc and strategy=shared. The single, already-existing PersistentVolumeClaim every user's home directory is a subdirectory of."
            }
          }
        }
      }
    },
    "ldap": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean", "description": "LDAP/Active Directory login, tried whenever a login isn't a valid local password. Local password login always stays available alongside it." },
        "url": { "type": "string", "description": "e.g. ldaps://dc1.example.com. Required when enabled." },
        "bindDn": { "type": "string", "description": "DN of the service account Helve binds as to search the directory. Required when enabled." },
        "existingSecret": { "type": "string", "description": "Secret (key: existingSecretKey) holding bindDn's password. Required when enabled." },
        "existingSecretKey": { "type": "string" },
        "baseDn": { "type": "string", "description": "Base DN to search under. Required when enabled." },
        "userFilter": { "type": "string", "description": "e.g. \"(uid={username})\" or \"(sAMAccountName={username})\". Required when enabled." },
        "adminGroupDn": { "type": "string", "description": "DN of a group whose membership maps to the admin role, re-checked on every login. Empty means no group mapping." },
        "autoProvision": { "type": "boolean", "description": "Whether a first-time LDAP login with no matching local account creates one automatically." }
      }
    },
    "oidc": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean", "description": "OIDC (SSO) login: an independent, simultaneously-usable alternative to local password and LDAP login." },
        "issuerUrl": { "type": "string", "description": "Required when enabled." },
        "clientId": { "type": "string", "description": "Required when enabled." },
        "existingSecret": { "type": "string", "description": "Secret (key: existingSecretKey) holding the OIDC client secret. Required when enabled." },
        "existingSecretKey": { "type": "string" },
        "usernameClaim": { "type": "string", "description": "ID token claim to read the Helve username from." },
        "groupsClaim": { "type": "string", "description": "ID token claim carrying group membership, read as a JSON array of strings." },
        "adminGroup": { "type": "string", "description": "A value in groupsClaim that maps to the admin role, re-checked on every login. Empty means no group mapping." },
        "autoProvision": { "type": "boolean", "description": "Whether a first-time SSO login with no linked local account creates one automatically." }
      }
    },
    "adminBootstrap": {
      "type": "object",
      "properties": {
        "existingSecret": { "type": "string", "description": "Existing Secret (key: password) to use instead of adminBootstrap.password." },
        "password": {
          "type": "string",
          "description": "Convenience only: creates the initial admin account on first boot against an empty database. Prefer adminBootstrap.existingSecret if this password shouldn't sit in your values file or Helm release history."
        }
      }
    },
    "nodeSelector": { "type": "object" },
    "tolerations": { "type": "array" },
    "affinity": { "type": "object" },
    "serviceAccount": {
      "type": "object",
      "properties": {
        "create": { "type": "boolean" },
        "name": { "type": "string" },
        "annotations": { "type": "object" }
      }
    },
    "rbac": {
      "type": "object",
      "properties": {
        "create": { "type": "boolean", "description": "Creates a namespace-scoped Role + RoleBinding. Never a ClusterRole." }
      }
    },
    "service": {
      "type": "object",
      "properties": {
        "type": { "type": "string", "enum": ["ClusterIP", "NodePort", "LoadBalancer"] },
        "port": { "type": "integer" }
      }
    },
    "resources": { "type": "object" },
    "extraEnv": { "type": "array" }
  },
  "required": ["host"]
}
